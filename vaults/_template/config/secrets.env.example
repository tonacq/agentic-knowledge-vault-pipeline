# Copy this file to secrets.env (same folder) and fill in real values.
# secrets.env is intentionally NOT committed/synced by sync-vault.ps1's Drive round-trip
# pattern for anything under config/ that you exclude — keep it out of source control.
#
# Leave blank / omit this file entirely to disable Telegram notifications for this vault.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
